# Streamlit Community Cloud / Hugging Face Spaces / Fly.io use this directly.
# Local development should `pip install -e ".[dev,dashboard]"` instead.

# Core package deps (mirrors pyproject.toml).
click>=8.1
pydantic>=2.5
python-dotenv>=1.0
anthropic>=0.40
openai>=1.40

# Dashboard deps.
streamlit>=1.30
plotly>=5.18
pandas>=2.1

# Install the local package itself so `from tom_benchmark import ...` works.
-e .
